module.exports = Object.freeze({
   COOKIE_SETTINGS: {
      REFRESH_TOKEN: {
         httpOnly: true,
         maxAge: 2592e6,
      }
   },
   ACCESS_TOKEN_EXPIRATION: 18e5,
});

const COOKIE_SETTINGS = {
   REFRESH_TOKEN: {
      httpOnly: true,
      maxAge: 2592e6,
   }
}

const ACCESS_TOKEN_EXPIRATION = 18e5;


